import { makeStyles } from '@mui/styles';

const useStyles: any = makeStyles({
  rootCell: {
    // color: '#446125',
    color: '#212B36',
    padding: '10px 10px',
    borderBottom: '2px #red solid'
  },
  celda: {
    borderBottom: '2px solid #c9c8c8'
  }
});

export default useStyles;
