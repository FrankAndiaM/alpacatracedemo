import { makeStyles } from '@mui/styles';

const useStyles: any = makeStyles({
  bodyCell: {
    padding: '10px',
    with: '200px',
    wordBreak: 'break-word'
  },
  textNoItems: {
    fontSize: '14px',
    padding: '15px',
    fontWeight: 'bold'
    // borderBottom: '0.14em solid #000000'
  },
  celda: {
    borderBottom: '1px solid #0000001d'
  }
});

export default useStyles;
